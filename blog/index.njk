---
layout: base.njk
title: Blog
description: News, stories, and updates from Golden Ticket Chocolates
---
<section class="section blog-hero">
  <div class="container text-center">
    <h1>Our Blog</h1>
    <p>News, stories, and updates from Golden Ticket Chocolates</p>
  </div>
</section>

<section class="section section-alt">
  <div class="container">
    <div class="blog-grid">
      {% for post in collections.posts %}
      <article class="blog-card">
        {% if post.data.image %}
        <div class="blog-card-image">
          <img src="{{ post.data.image }}" alt="{{ post.data.title }}">
        </div>
        {% else %}
        <div class="blog-card-image blog-card-placeholder">
          <span>Blog Post</span>
        </div>
        {% endif %}
        <div class="blog-card-content">
          <p class="blog-card-date">{{ post.date | dateFormat }}</p>
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          <p class="blog-card-description">{{ post.data.description }}</p>
          <a href="{{ post.url }}" class="btn btn-outline">Read More</a>
        </div>
      </article>
      {% else %}
      <p class="text-center">No blog posts yet. Check back soon!</p>
      {% endfor %}
    </div>
  </div>
</section>

<section class="cta-banner">
  <div class="container">
    <h2>Want to Stay Updated?</h2>
    <p>Follow our journey as we make a difference, one chocolate at a time.</p>
    <a href="/contact/" class="btn btn-outline">Get in Touch</a>
  </div>
</section>
